'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function New(){const[n,setN]=useState('');const router=useRouter();async function create(){const r=await fetch('/api/games',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:n})});const d=await r.json();if(!r.ok)return alert(d.error);router.push(`/game/${d.code}?name=${encodeURIComponent(n)}&host=1`)}return <main className="container"><div className="card" style={{maxWidth:500,margin:'60px auto'}}><h1>Créer une partie</h1><p style={{color:'#a4a8b3'}}>Pas besoin de compte pour jouer.</p><input value={n} onChange={e=>setN(e.target.value)} placeholder="Ton pseudo" style={input}/><button onClick={create}>Créer la salle</button></div></main>}const input={width:'100%',padding:14,margin:'8px 0',borderRadius:10,border:'1px solid #303441',background:'#0b0c11',color:'white'};
