'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function Join(){const[r,setR]=useState('');const[n,setN]=useState('');const router=useRouter();return <main className="container"><div className="card" style={{maxWidth:500,margin:'60px auto'}}><h1>Rejoindre une partie</h1><input value={r} onChange={e=>setR(e.target.value.toUpperCase())} placeholder="Code de salle" style={input}/><input value={n} onChange={e=>setN(e.target.value)} placeholder="Ton pseudo" style={input}/><button onClick={()=>router.push(`/game/${r}?name=${encodeURIComponent(n)}`)}>Rejoindre</button></div></main>}const input={width:'100%',padding:14,margin:'8px 0',borderRadius:10,border:'1px solid #303441',background:'#0b0c11',color:'white'};
