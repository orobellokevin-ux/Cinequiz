'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function Login(){const[u,setU]=useState('');const[p,setP]=useState('');const[r,setR]=useState('');const router=useRouter();async function go(){const x=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:u,password:p})});const d=await x.json();if(!x.ok)return setR(d.error);router.push('/')}return <main className="container"><div className="card" style={{maxWidth:500,margin:'60px auto'}}><h1>Connexion</h1><input placeholder="Pseudo" value={u} onChange={e=>setU(e.target.value)} style={input}/><input placeholder="Mot de passe" type="password" value={p} onChange={e=>setP(e.target.value)} style={input}/><button onClick={go}>Se connecter</button>{r&&<p>{r}</p>}<p><a href="/register">Créer un compte</a></p></div></main>}const input={width:'100%',padding:14,margin:'8px 0',borderRadius:10,border:'1px solid #303441',background:'#0b0c11',color:'white'};
